# 炉石传说自动对战 Bot 任务清单

## 1. 当前范围

- 运行环境固定为 Windows 前台截图 + 前台输入
- 当前支持两个固定分辨率 profile：`1600x900`、`1440x900`
- 当前仍不支持动态分辨率适配
- 游戏模式为窗口化，窗口目标位置为 `(20, 20)`
- 目标模式为休闲模式
- 通过全局热键 `F8` 停止

## 2. 当前运行方式

- 主程序：
  - `python -m bot.main --deck-index 1 --resolution 1600x900`
  - `python -m bot.main --deck-index 1 --resolution 1440x900`
- 采样工具：
  - `python -m bot.sample_main --tag in_battle --deck-index 1 --resolution 1600x900`
  - `python -m bot.sample_main --tag in_battle --deck-index 1 --resolution 1440x900`

说明：

- `deck_index` 范围为 `1-9`
- `match_count` 已移除

## 3. 已完成

- [x] 支持窗口归位和客户端区域尺寸校验
- [x] 支持中文标题 `炉石传说` 和英文标题 `Hearthstone`
- [x] 已建立 `regions / templates / OCR` 配置加载链路
- [x] 已建立采样工具，支持按 tag 保存完整截图和区域裁图
- [x] 已跑通 `startup -> main_menu -> battle_menu -> queue_page -> mulligan -> battle -> result`
- [x] 已支持 `deck_index=1-9`
- [x] 已支持基础出牌、结束回合、结算继续
- [x] 已支持 `1600x900` 资源
- [x] 已支持 `1440x900` 资源
- [x] 已去掉 `queue_play_button` / `end_turn` 的固定像素偏移
- [x] 已将 `traditional_battle_button` 点击锚点下移，避开按钮上沿装饰
- [x] 已增加关键点击与战斗动作日志
- [x] 已增加双 watchdog
- [x] 已重构 `bot/vision/board_state.py` 的手牌检测链路
- [x] 已引入 `hand_detection` YAML 配置
- [x] 已实现宽连体高亮按横向峰值拆中心
- [x] 已实现自适应去重，改善密集手牌识别
- [x] 已完成 `1440x900` 多组 battle 样本离线校准
- [x] 已新增匹配失败弹窗 `match_error` 处理
- [x] 已将 `confirm_dialog / result / result_continue / match_error` 的识别与恢复逻辑收敛到 `runtime.py`
- [x] 已增加场景归一化原因日志

## 4. 当前资源文件

- [x] `bot/config.py`
- [x] `bot/cli.py`
- [x] `bot/runtime.py`
- [x] `bot/regions/1600x900.yaml`
- [x] `bot/regions/1440x900.yaml`
- [x] `bot/templates/1600x900/templates.yaml`
- [x] `bot/templates/1440x900/templates.yaml`
- [x] `bot/ocr/1600x900.yaml`
- [x] `bot/ocr/1440x900.yaml`

## 5. 当前问题

- [ ] 战斗内手牌候选识别仍可能误检或漏检
- [ ] 出牌策略仍是近似规则，不是真正费用驱动
- [ ] `1440x900` 仍需要继续用实机样本校准
- [ ] 结果页与异常弹窗边界仍需要继续压缩误判
- [ ] 仍不支持动态分辨率适配
- [ ] watchdog 仍需继续观察误触发和漏触发情况

## 6. 后续优先级

- [ ] 继续优化 `1440x900` 的 battle / result / matching / match_error 识别稳定性
- [ ] 恢复费用数字 OCR
- [ ] 增加更稳的出牌目标和攻击逻辑
- [ ] 继续重构结果页与异常恢复逻辑
- [ ] 继续观察并微调 watchdog 的超时与结果页判定阈值

## 7. 当前交接重点

手牌检测：

- 已不再使用旧的超宽连通域等宽切块
- 当前主链路为：绿色掩码 -> 原始候选 -> 宽连体拆峰值 -> 去重 -> 可出评分
- 阈值在 `bot/regions/*.yaml` 的 `hand_detection` 段

场景与恢复：

- `bot/vision/scene.py` 中 `confirm` 现在先归类为 `confirm_dialog`
- `bot/runtime.py` 负责将其结合上下文归一化为：
  - `match_error`
  - `result`
  - `result_continue`
- 实机调试时应优先观察归一化原因日志

## 8. 建议后续顺序

1. 继续用实机样本验证 `1440x900` 下的场景归一化和异常恢复
2. 恢复 mana OCR，只做费用识别
3. 增加更完整的出牌与攻击逻辑
4. 最后再考虑更复杂的战斗规则
